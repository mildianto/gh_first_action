name: First Workflows
on:
  push:
    branches:
      - main
    paths:
      - /**
  #workflow_dispatch:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbye
        run: |
          echo "Done - bye!"
          echo "Selamat tinggal"
